# Бот для прохождения тестирования hrbot

## Описание
Бот для прохождения тестирования.
Бот предназначен для автоматизации процесса прохождения тестов и получения результатов тестирования.


В ходе разработки особое внимание уделяется качеству кода, прозрачности процесса,
документированию решений и регулярной обратной связи через code review.

---

## Цель проекта
Цель проекта — разработка работоспособного бота для прохождения тестирования, а также
формирование практических навыков разработки программного обеспечения, включая:

- анализ требований и формирование логики работы бота
- работу с Git и GitHub (issues, ветки, pull requests)
- написание читаемого и поддерживаемого кода
- обработку ошибок и нестандартных ситуаций
- документирование функциональности и решений
- получение и внедрение обратной связи от наставника

---
## Архитектура проекта
Подробная архитектура: [docs/architecture.md](docs/architecture.md)

---

# Руководство по запуску проекта

Этот документ описывает, как запустить приложение, настроить окружение, установить зависимости и понять общую историю разработки.

---

# 📦 Установка и запуск

## 1. Клонирование репозитория

```
git clone https://github.com/googlaev/hrbot.git
cd hrbot
```

---

# 🧰 Установка зависимостей

Проект использует Python 3.12+.

## 2. Создайте виртуальное окружение

```
python -m venv venv
source venv/bin/activate     # Linux / Mac
venv\Scripts\activate        # Windows
```

## 3. Установите зависимости

```
pip install -r requirements.txt
```

---

# ⚙️ Настройка переменных окружения

Проект ожидает файл `.env` в корне.

Пример находится в директории `examples/.env.example`.

Создайте свой `.env`:

```
TIMEZONE="Europe/Moscow"
TG_BOT_TOKEN="ВАШ_ТОКЕН_ОТ_BotFather"
DEBUG_ENABLED="1/0"
```

---

# ▶️ Запуск приложения

```
python src/main.py
```

---

# 🧪 Запуск тестов

```
pytest
```

# Инструмент управления пользователями (`tools/manage_users.py`)

Консольный скрипт для просмотра, создания, изменения и удаления пользователей в базе данных.

**Запуск:**

```
python -m tools.manage_users
```

Доступные команды внутри инструмента:

| Команда	      | Описание                      |
| --------------- | ----------------------------- |
| list	         | - показать всех пользователей |
| set <id> <role>	| - изменить роль               |
| exit	         | - закрыть программу           |

---

# 📚 Используемые библиотеки

Ниже перечислены основные библиотеки, применяемые в проекте, и их назначение:

### **1. aiogram**

Фреймворк для создания Telegram-ботов. Отвечает за:

* обработку апдейтов,
* маршрутизацию команд,
* middleware,
* фильтры и хендлеры.

### **2. openpyxl**

Библиотека для чтения и обработки Excel-файлов (`.xlsx`). Используется парсером тестов.

### **3. aiosqlite**

Используются для:

* управления SQLite-базой,
* запросов,
* миграций.

### **4. python-dotenv**

Загружает переменные окружения из `.env`.

### **5. pytest**

Фреймворк для тестирования. Применяется для:

* unit-тестов парсеров,
* тестирования логики use-case,
* фикстур.

### **6. pytz**

Работа с таймзонами.

---

# 📜 История разработки

Проект создавался как обучающий и практический пример использования **чистой архитектуры (Clean Architecture)** с интеграцией Telegram-бота и системой тестов.

Этапы развития:

1. **Изначально** был простой Telegram-бот с минимальным функционалом и без архитектурного разделения.
2. **Позже** было решено внедрить Clean Architecture, чтобы:
   * разделить ответственность по слоям,
   * упростить тестирование,
   * облегчить расширение проекта.
3. Добавлены:
   * работа с Excel-файлами (парсинг тестов),
   * слои портов и адаптеров,
   * полноценная доменная модель пользователей и ролей.
4. Далее появилась система прав администратора.
5. Потом — тесты и фикстуры.

На текущем этапе проект представляет собой полноценный каркас со стабильными модулями, удобной структурой и возможностью дальнейшего масштабирования.

---



